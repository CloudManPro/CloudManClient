#Git account name: CloudManPro
#CloudManClient/.github/workflows/cloudman-deploy.yml
name: Cloudman Pipeline
on:
  push:
    paths: ['**/manifest.json']

jobs:
  infrastructure:
    # Chamada para o repositório central da CloudManPro
    uses: CloudManPro/cloudman-core/.github/workflows/engine.yml@main
    with:
      aws_region: "us-east-1"
    secrets:
      AWS_ROLE_ARN: ${{ secrets.AWS_ROLE_ARN }}
